public class JavaEightFeature {

  // 1995 -> C/C++ -> Java 1.0

  // 2014 ---> 4~6   2018~2020 jdk8

  // lambda expression --> functional programming --> anonymous method --> compararlbe vs comparator
  // functional interface vs interface
  // Consumer, Supllier, Predicate, Function
  // 1input/0ouput, 0input/1output, input/boolean, input1/1output

  // default method in interface
  // static method in interface
  // interface -> impl1(method1), impl2(method1), impl3(method1), impl4(method1) --> Don't Repat Yourself

  // stream api -> intermediate vs terminal
  // intermediate -> stateful(sort, distinct) stateless
  // for vs stream API // List<Integer>.stream().mapTOInteger(method references/functional interface/lambda expression).map(logic).flatMap(logic).forEach()
  // method references

  // compariable vs comparator -> figure out

  // SimpleDateFormat (deprecated)

  // Google Guava Lib
  // optional
  // wrapper -> OrElse, isEmpty etc...
  // NullPointerException -> grab data remote DB, Employee -> null
  // null.getFirstname()
  // Optional<Employee> emplyee
//  employee.orElse (DefaultEmployee).getFirstname;''

  // # to-do
  // stream API practice
  // recording for simulations of all topics we discussed on all days sessions
  // what is Thread
  // Thread vs process
  // thread creation -> Thread vs Runnable vs Callable
  // thread lifecycyle
  // thread pool
  // thread control -> wait, sleep, notify, notifyAll
  // thread safety -> synchronized, Lock interfaxe
  // CAS -> non-locking mecahnism
  // voatile keyword



  public static void main(String[] args) {

  }
}
